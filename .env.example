# Supabase Configuration (Public - safe in frontend)
VITE_SUPABASE_URL=https://your-project-id.supabase.co
VITE_SUPABASE_ANON_KEY=your-anon-key

# App Configuration (Public - safe in frontend)
VITE_APP_URL=https://www.ai-travelstudio.nl

# ============================================
# SERVER-ONLY SECRETS (Never use VITE_ prefix!)
# These are configured via Supabase Dashboard → Project Settings → Edge Functions
# OR stored in api_settings table in database
# ============================================

# JWT Secret (Server-only - used by edge functions)
JWT_SECRET=your-jwt-secret-key

# OpenAI Configuration (Server-only - stored in api_settings table)
# Configure via: Operator Dashboard → API Settings

# Google APIs Configuration (Server-only - stored in api_settings table)
# Configure via: Operator Dashboard → API Settings

# Unsplash Configuration (Server-only - stored in api_settings table)
# Configure via: Operator Dashboard → API Settings

# YouTube Configuration (Server-only - stored in api_settings table)
# Configure via: Operator Dashboard → API Settings

# Twilio Configuration (Server-only - stored in api_settings table)
# Configure via: Operator Dashboard → API Settings